flask>=3.0.0
flask-mail>=0.10.0

# Standard library modules used (no install needed):
#   sqlite3, hashlib, re, os, datetime
#
# Email OTP requires these environment variables:
#   SMTP_HOST, SMTP_PORT (default 587), SMTP_USER, SMTP_PASS, SMTP_FROM (optional)
